# increff-adidas-return
The tool which takes input as csv file with return expectations and without return expectations , process and return. 

git add .
git commit -m "Your Message"
git push origin main
